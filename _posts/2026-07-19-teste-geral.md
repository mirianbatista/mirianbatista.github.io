---
layout: post
title: Post de teste
date: 2026-07-19 22:00:00 +0000
categoria: engenharia de dados
tagline: "Página temporária única: valida o layout, a categoria e a entrega por email de uma vez. Some quando o primeiro post real chegar."
summary: "Página temporária única: valida layout, categoria e entrega por email. Some quando o primeiro post real chegar."
---
Se você assinou o feed por email, este post deve ter aparecido na sua caixa de entrada; é a validação de ponta a ponta do caminho de publicação. O resto da página confere os elementos comuns de um post técnico.

## Um subtítulo de seção

Texto com um [link](/), um trecho de `codigo_inline()` e um bloco de código:

```
def exemplo(pipeline: str) -> str:
    return f"processando {pipeline}"
```

### Uma lista

- Primeiro item da lista
- Segundo item, um pouco mais longo pra ver a quebra de linha se comportando

> Uma citação destacada, pra quando o post referenciar uma fonte.

E um parágrafo final fechando o post.
